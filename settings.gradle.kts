pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "import_error_sample"
include(":androidApp")
include(":shared")
include(":other")
